#include <stdio.h>
#include <cs50.h>

int main (void)
{
    int n = number();
    int score[n];
}

int number(void)
{
    int x=get_int("How many number do you want to find the average for? ");
    return x;
}

int average(int a)

{
    for (int i=0;i<a;i++)
    {
        
    }
}